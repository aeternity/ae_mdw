name: 'Mdw setup'

inputs:
  deploy-key:
    required: true

runs:
  using: "composite"
  steps:
    - uses: erlef/setup-beam@v1
      with:
        otp-version: 23.3.4.5
        elixir-version: 1.11.4

    - name: Get Mdw dependencies
      shell: bash
      run: mix deps.get